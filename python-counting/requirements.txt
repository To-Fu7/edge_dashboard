# Thin Triton client — no torch/ultralytics/tensorrt (those live only in
# tools/Dockerfile.export and the tritonserver image).
tritonclient[grpc]>=2.41.0
numpy>=1.26.0
opencv-python-headless>=4.9.0
scipy>=1.11.0
lapx>=0.5.10.post1
psycopg2-binary
python-dotenv
paho-mqtt
